import React, { useEffect, useState } from "react";

const student = (props) => {
  return (
    <>
      <h1>student Page</h1>
      {/* //render up to 25 students from the database for the chosen campus 
           have a delete button associated with each div/ student element being
           rendered, where we would dispatch the dleete student action
           using an event handler that is attached to the button delete
      
      
      */}
    </>
  );
};

export default student;
